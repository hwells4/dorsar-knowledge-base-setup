# Preferences

One file per teammate, named after them: `preferences/harrison.md`. It holds how that one person likes things done (summary style, tone, which client they touch most) and only shapes their own sessions. The agent appends to it when the person states a personal habit, and reads it after the team rules in `SKILL.md`. Committed with everything else so it follows the person across machines.
